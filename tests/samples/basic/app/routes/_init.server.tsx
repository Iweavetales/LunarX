import { ServerContext } from "lunarx/server"
// import { fileLogger } from "../../../../../packages/lunarx/core/logging.server"

export default async function (ctx: ServerContext) {
  // console.log(fileLogger.info({ title: "test" }))
  return true
}

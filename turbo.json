{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "inputs": [
        "../../packages/**/*.ts"
      ],
      "dependsOn": ["build:deno-runtime", "^build"]
    },
    "build:deno-runtime": {
      "dependsOn": ["^build:deno-runtime"]
    }
  }
}

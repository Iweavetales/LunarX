{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build", "build:deno-runtime"]
    },
    "build:deno-runtime": {
      "dependsOn": ["^build:deno-runtime"]
    }
  }
}
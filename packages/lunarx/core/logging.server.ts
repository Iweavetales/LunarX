import pino from "pino"

// export const logger = (() => {
//
//     if( process.env.LOG_DIR ){
//         return pino(pino.destination("./logs/default.json"))
//     } else{
//         return pino()
//     }
// }))()
